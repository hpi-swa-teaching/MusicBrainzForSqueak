A MBWindow is an abstract superclass that has the interface for basic ToolBuilder window functions.

Instance Variables
	builder:		ToolBuilder

builder
	- The ToolBuilder must be called in some methods
